{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() ~ title }}
{% endblock %}

{% block body %}

    <ul>
        {% for sortie in sorties %}
            <li>
                {{ sortie.nom }}
            </li>
        {% endfor %}
    </ul>
{% endblock %}
